import type {
  BookingModeConfig,
  PlanToBookingHandoff,
} from "@/components/booking/shared/bookingLabTypes"

export default function ExperiencesBookingMode(_props: {
  config: BookingModeConfig
  planHandoff?: PlanToBookingHandoff | null
}) {
  return null
}